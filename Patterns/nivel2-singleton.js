const game = () => {
    let players = [];

    addPlayer(player) {
        players = [...players, player];
        console.log(players);
    }
};

const player = (name) => {

    constructor() {
        this.name;
        this.points = 0;
        game.addPlayer('Marcel');
    }

}

const Singleton = (() => {
    var instance;

    function createInstance() {
        var score = new Object();
        score.players
    }
})();

game = new game();

const player1 = 'Marcel';
player(player1);